---
import BlogIndex from '../../../components/page/BlogIndex.astro';
import { NON_DEFAULT_LANGS } from '../../../i18n/config';

export function getStaticPaths() {
  return NON_DEFAULT_LANGS.map((lang) => ({ params: { lang } }));
}
---

<BlogIndex />
