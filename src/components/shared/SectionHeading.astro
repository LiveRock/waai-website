---
interface Props {
  title: string;
  subtitle?: string;
  badge?: string;
  align?: 'center' | 'left';
  /** Heading level — 'h1' for pages whose SectionHeading is the only page heading. */
  as?: 'h1' | 'h2';
}

const { title, subtitle, badge, align = 'center', as = 'h2' } = Astro.props;

const alignment = align === 'center' ? 'text-center mx-auto' : 'text-left';
const Heading = as;
---

<div class:list={['max-w-3xl mb-12', alignment]}>
  {badge && (
    <span class="inline-block px-3 py-1 text-xs font-semibold text-wa-green bg-green-50 rounded-full uppercase tracking-wider mb-4">
      {badge}
    </span>
  )}
  <Heading class="text-3xl sm:text-4xl font-bold text-gray-900 mb-4">{title}</Heading>
  {subtitle && (
    <p class="text-lg text-gray-600 leading-relaxed">{subtitle}</p>
  )}
</div>
