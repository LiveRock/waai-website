---
---

<section class="py-12 border-b border-gray-100 overflow-hidden">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mb-8">
    <p class="text-center text-sm font-medium text-gray-500 uppercase tracking-wider">Trusted by businesses worldwide</p>
  </div>
  <div class="relative">
    <div class="flex animate-scroll">
      {[1,2,3,4,5,6,7,8].map(() => (
        <div class="flex items-center gap-16 px-8 shrink-0">
          {[1,2,3,4].map((i) => (
            <div class="flex items-center justify-center h-8 px-6 opacity-40 hover:opacity-100 transition-opacity grayscale hover:grayscale-0">
              <div class="w-24 h-6 bg-gray-300 rounded"></div>
            </div>
          ))}
        </div>
      ))}
    </div>
  </div>
</section>
